import 'package:flutter/material.dart';

class PlaceOrder_page extends StatelessWidget {
  const PlaceOrder_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
